import { Route } from '@angular/router';
import { EntryListComponent } from './index';

export const EntryListRoutes: Route[] = [
    {
      path: 'entry-list',
      component: EntryListComponent
    }
];
