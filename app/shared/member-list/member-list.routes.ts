import { Route } from '@angular/router';
import { MemberListComponent } from './index';

export const MemberListRoutes: Route[] = [
    {
      path: 'member-list',
      component: MemberListComponent
    }
];