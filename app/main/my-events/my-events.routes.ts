import { Route } from '@angular/router';
import { MyEventsComponent } from './index';

export const MyEventsRoutes: Route[] = [
    {
      path: 'my-events',
      component: MyEventsComponent
    }
];
